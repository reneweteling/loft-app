<section id="security">
  <div class="section-inner">
    <p class="section-label">First launch</p>
    <h2 class="section-heading">Signed and notarized.</h2>

    <div class="intro">
      <p>
        Loft releases are signed with an Apple <strong>Developer ID</strong> and
        <strong>notarized by Apple</strong>. macOS opens the app like any other download:
        no Gatekeeper warnings, no "Open Anyway" detour, no terminal tricks. Download,
        drag to Applications, done.
      </p>
      <p class="note">
        Running a release older than v1.6.0? Those builds were ad-hoc signed. Grab the
        latest release and the warnings are gone.
      </p>
    </div>
  </div>
</section>

<style>
  .intro p {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.7;
    max-width: 680px;
  }
  .intro strong {
    color: var(--text);
    font-weight: 600;
  }
  .intro .note {
    margin-top: 1rem;
    font-size: 0.85rem;
  }
</style>
